<!-- 计算器页面 -->
<view class="calc-page">
  <!-- 顶部导航（计算 | 换算） -->
  <top-nav active="calc" />

  <!-- 显示区域 -->
  <display-area
    expression="{{expression}}"
    result="{{result}}"
    error="{{error}}"
  />

  <!-- 数字键盘 -->
  <numpad
    layout="basic"
    bind:digit="onDigit"
    bind:operator="onOperator"
    bind:clear="onClear"
    bind:backspace="onBackspace"
    bind:equals="onEquals"
    bind:percent="onPercent"
    bind:negate="onNegate"
    bind:decimal="onDecimal"
  />
</view>
