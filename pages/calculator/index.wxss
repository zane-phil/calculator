/* 计算器页面样式 */
.calc-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background-color: #F7F7F7;
}

/* 显示区域占剩余空间 */
.calc-page > display-area {
  flex: 1;
}

/* 键盘区域 */
.calc-page > numpad {
  flex-shrink: 0;
}
