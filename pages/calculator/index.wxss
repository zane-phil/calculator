/* 计算器页面样式 */
.calc-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background-color: #F7F7F7;
}

.calc-page > display-area {
  flex: 1;
}

.calc-page > numpad {
  flex-shrink: 0;
}
